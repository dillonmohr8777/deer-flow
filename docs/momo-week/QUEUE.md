# Momo Week queue (2026-09-24 → 2026-10-01)

Worked by a scheduled Claude Code cloud routine, one task (or one resumable slice) per run.
Integration branch: `lane/momo-week` (cut from `integrate/m1-20260922`).
Receipts: `docs/momo-week/PROGRESS.md`. Owner: Claude chief for Dillon Mohr.

## Rules for every run

1. Take the first task marked `[~]` (in progress) or else the first `[ ]`. Never skip ahead past a `[!]` unless the blocker note says it is independent.
2. Branch `momo-week/<slug>` from the latest `lane/momo-week`. Open or update a **draft PR into `lane/momo-week`**. Never target `main`, `production` or `integrate/*`. Never merge anything.
3. Done means the task's acceptance check passes in this run's Linux sandbox (backend: `cd backend && uv run pytest <paths> -q`; frontend: `cd frontend && pnpm test <paths>`). No green check, no `[x]`.
4. If a task is too big for one run, leave it `[~]`, push what works behind passing tests, and write the next step in PROGRESS.
5. Mark `[!]` with a one-line reason when blocked on something only Dillon can give: credentials, a product decision, spend, a deploy, a live instance. Then move to the next independent task in the same run if time allows.
6. No secrets, no deploys, no `.env` edits, no touching `deploy/momentum/*.yaml` image pins, no external sends.
7. Finish by committing the updated QUEUE.md and a PROGRESS.md line **to `lane/momo-week` directly** (these two files only).
8. Three routines share this file. **Build routine** (`momo-week`): sections A, B, C, E and F. **Design routine** (`momo-week-design`): section D only. **Review routine** (`momo-week-review`): adds tasks to section F only and never takes tasks. Never take a task from the other routine's sections. When your sections have no `[ ]` or `[~]` left, log `queue empty` in PROGRESS and stop.

## Tasks

### A. M3 isolation gate (prerequisite for anything multi-client)
- [x] **a1 m3-audit**: https://github.com/dillonmohr8777/deer-flow/pull/17 — all 7 gate-7.2 gaps from the plan are already closed on this branch (see `docs/momo-week/m3-status.md`); one unrelated pre-existing suite failure noted (`test_client_langfuse_metadata.py`), not caused by this change.
- [x] **a2 m3-gaps**: dropped by the Claude chief 2026-09-24 23:50 UTC. a1 found no gate-7.2 gaps. The pre-existing `test_client_langfuse_metadata.py` failure is not isolation work; leave it alone.

### B. Momo Board (Workspace Phase 4): client posts, tickets, concerns, DMs, Momo drafts, owner approves
Reuse: `backend/app/gateway/routers/clients.py`, the org/membership model, the Desk page (`frontend/src/app/workspace/desk/`), run receipts, and the momo-concierge prompt text (triage, draft only, label claims observed/inferred/unknown). Gate everything behind `config.private_workspace.enabled`, the same way the Desk does.
- [x] **b1 board-model**: https://github.com/dillonmohr8777/deer-flow/pull/19 — `board_threads` + `board_messages` tables added (migration `0038_board_threads`, chains after `0037_pat_organization`, single head), `BoardThreadKind`/`BoardThreadStatus` StrEnums, models registered so `create_all` matches the alembic chain for these tables.
- [x] **b2 board-api**: https://github.com/dillonmohr8777/deer-flow/pull/22 — `BoardRepository` (org-scoped, mirrors `ClientRepository`) plus `/api/board` CRUD router (threads + messages); per-client isolation via `client_assignments` + org-admin check, mirroring `clients.py`'s stamp-authorization pattern. Branch carries b1's unmerged model commit, since nothing merges into `lane/momo-week` this week.
- [x] **b3 board-triage**: https://github.com/dillonmohr8777/deer-flow/pull/23 — `deerflow.board.triage.triage_board_thread()` classifies kind/urgency/summary via `create_chat_model` + `ainvoke` (mirrors `skills/security_scanner.py`'s JSON-response pattern), ephemeral (no urgency/summary column exists yet, so nothing is persisted here), falls back to a safe default on model failure. Branch carries b1+b2 forward. Left: wiring it into thread creation/status transitions is b4's job.
- [x] **b4 board-draft-approve**: https://github.com/dillonmohr8777/deer-flow/pull/25 — `deerflow.board.workflow`'s pure `assert_can_draft/approve/reply` state machine (new|triaged -> drafted -> approved -> replied), wired into `/api/board/threads/{id}/draft|approve|reply`; approve/reply reuse `board.py`'s existing `_is_active_org_admin` check (403 for a non-owner even with client access), every transition writes an `audit_events` row. Branch carries b1+b2+b3 forward. Left: wiring b3's triage into thread creation, and b5's UI.
- [x] **b5 board-ui**: https://github.com/dillonmohr8777/deer-flow/pull/26 — `/workspace/board`: thread list by status, thread view with message history, draft editor, approve button, send-reply step, in the Desk's visual language, gated behind `features.desk.enabled` (sidebar link added next to Desk's). `core/board` wraps the `/api/board` contract from b2/b4; UI action gating mirrors `deerflow.board.workflow`'s state machine, backend stays the source of truth (403/409 surfaces inline). Branch cut from `lane/momo-week` directly (b1-b4's code only exists on their own unmerged branch); exercised against a mocked API. Left: no thread-creation UI (out of scope per task text); d8 board-visual (queued) brings it into the deeper paper language.
- [x] **b6 scenario-catalog**: https://github.com/dillonmohr8777/deer-flow/pull/29 — `backend/tests/fixtures/board_scenarios.yaml`: 8 invented clients (one per Momentum service line, mixed size/temperament, `.example` domains only), 5 threads each (40 total) covering all 4 kinds and all 15 required situations incl. prompt injection and must-refuse; `board_scenarios.schema.json` + `seed_board_scenarios.py` (loads via the real `ClientRepository`/`BoardRepository` into a temp SQLite DB, also runnable standalone). Left: b7 wires the catalog through the real triage/draft/approve/reply pipeline with a stubbed model; b8 writes the report from that run.
- [x] **b7 scenario-e2e**: https://github.com/dillonmohr8777/deer-flow/pull/30 — `test_board_scenario_e2e.py` runs all 40 catalog threads through the real `/api/board` router (post → triage, stubbed from each thread's own `expected` block → draft-or-escalate → owner approve → reply); `BoardThreadTriage` gains an `action` field (draft|escalate, default draft, falls back to escalate on failure) since triage itself has to make that call. Asserts status/urgency/action match `expected`, no forbidden phrase ever lands in a sent/drafted message, must-refuse/injection never reach approved/replied, audit trail complete for every drafted thread, and (second test, same built world) no client's assigned staff ever sees another client's threads/messages/name. 60/60 targeted tests pass, printed scenario/expected/got table all "yes"; 713 passed/17 skipped on `-k "board or client"` sweep (pre-existing unrelated `test_client_langfuse_metadata.py` failure, same one a1/b1/b2/b3/b4/b6 already noted); ruff check+format clean. Left: b8 turns the weakest fixtures into a report; e2 persists urgency/summary/action and wires triage into thread creation.
- [x] **b8 scenario-report**: https://github.com/dillonmohr8777/deer-flow/pull/35 — `docs/momo-week/scenario-report.md`: pass/fail table for all 40 catalog scenarios (40/40 pipeline pass); names the 14 `draft`-action scenarios (onboarding, report-explain, site-down, ad-spend-worry, praise-referral) that get the same generic canned reply regardless of content, since draft-content generation doesn't exist yet, plus flags the 9 highest-stakes escalate scenarios (must-refuse, prompt-injection, legal-compliance, data-privacy) exposed to the still-open enforcement gap (f13); prioritized gap list (enforce escalate-blocks-draft, persist urgency/summary/action, after-hours signal, draft generation, billing handoff, referral capture). Branch carries b6+b7 forward (same pattern as b7 carrying b6). Section B is now fully done.

### C. Upkeep
- [!] **c1 upstream-sync**: blocked — merge attempted (`upstream/main` @ `74f6dce4`, 101 ahead/403 behind), 21 files conflicted before abort; 5 backend conflicts sit inside the auth/org-isolation layer itself (`auth/repositories/sqlite.py`'s private-org-on-signup insert, `routers/agents.py`'s per-user agent scoping, `routers/user_preferences.py`, `services.py`, `config/paths.py`) plus the MCP task persistence layer. Per this task's own rule, stopped and wrote `docs/momo-week/upstream-conflicts.md` (full file list, why each backend hunk is isolation-sensitive, 3 options) instead of guessing at a resolution. Needs a Dillon decision on scope (partial sync vs. full hand-merge vs. skip) before any run attempts this again.
- [x] **c2 m4-entitlement-design**: https://github.com/dillonmohr8777/deer-flow/pull/45 — `docs/momo-week/m4-entitlement.md`: `organization_entitlements` schema (contract §3), a `require_entitlement` decorator composed directly after the existing `require_permission` in `app/gateway/authz.py` reusing `AuthContext.organization_id`, single-owner-org rollout with no billing service yet (manual entitlement rows), the effective-entitlement snapshot endpoint shared between display and enforcement, and test cases for e6's acceptance bar. Review found f22 (confirmed): the doc's decorator example stacked `@require_auth` above `@require_permission`, which overwrites `request.state.auth` and loses `organization_id` (no router uses `@require_auth` today); fixed in 675181bd — dropped `@require_auth`, a request with no resolved org is now documented as denied (403) not skipped, and the degraded-provider cache is now explicitly process-level (not "cached this request cycle"), plus both cases added to the e6 test list. Left: e6 implements it; open questions for Dillon (default `*.max` limits, whether `console.read` is ever deniable pre-billing, where the admin write path lives) noted in the doc.

### D. Design (design routine only)
Source of truth: `DESIGN.md` ("MomoBot Paper": its tokens, type and rules win over any other taste). Findings to fix: `plans/momentum-design-round4-backlog.md` (Astra's review of the live release, 7/10 cohesion). Reference surfaces built 2026-09-24: the Desk (`frontend/src/app/workspace/desk/`, PR #15) and the signed-in polish pass (PR #13, before/after images in `docs/pr-evidence/`).

**How every design run works:**
- Screenshot the target route before the change with Playwright against the mocked API. Use the existing `frontend/tests/e2e` setup (`pnpm exec playwright install chromium` if needed), at 1440×900 and 390×844.
- Make the change, screenshot again, and commit both to `docs/pr-evidence/momo-week/<slug>/`.
- Critique the "after" shots against DESIGN.md and the backlog item, as a strict reviewer. Fix what fails, then put a 1–10 score with its reasons in the PR body.
- Accept: `pnpm test` for touched components and `pnpm lint` pass, before/after images committed, and every text/background pair touched meets WCAG AA.

- [x] **d1 metadata-legibility** (backlog 4): https://github.com/dillonmohr8777/deer-flow/pull/18: run metadata, status, chips and composer labels at 13 to 14px with stronger weight; sidebar and agent icon controls at 40px. Left: the sidebar thread-row action is still 20px on desktop.
- [x] **d2 work-titles** (backlog 2): https://github.com/dillonmohr8777/deer-flow/pull/20: two-line titles plus a time/day and project identifier on Latest assignments, sidebar Recent chats and the Chats page; native tooltip on hover, focus unclamps. Left: the sidebar scraps footer now limits Recent chats to ~3.5 rows at 900px.
- [x] **d3 telemetry-labels** (backlog 3): https://github.com/dillonmohr8777/deer-flow/pull/24: Command Center totals carry scope/period lines ("Right now", "All time, your runs", "All time, input and output"); zero tokens across recorded runs, run rows and the receipt read "Not recorded"; chat pill names units ("48.2K tokens", "36.4% context"), hides a percentage with no known window, says "Tokens unavailable"; menu gains a Context window meter with count/max and a one-line meaning. Left: usage menu Display presets and footnote still 12px and engineer-voiced; top bar says "Your workspace" while stats are per-user.
- [x] **d4 agent-identity** (backlog 8): https://github.com/dillonmohr8777/deer-flow/pull/27: agent chat header badge and welcome now draw the agent's `MomoAvatar` (was a generic lucide robot) so roster, Command Center and chat match; gold confined to the antenna (four Momo rivets regenerated grey, MomentumGlyph brass pin removed); DESIGN.md rule added. Left: custom agents without canon art keep the ink monogram disc (a grey-tool variant needs a product decision); sidebar Agents nav still uses the lucide bot as a nav icon.
- [x] **d10 agents-alive**: https://github.com/dillonmohr8777/deer-flow/pull/34: Command Center agent team avatars show recorded run state via `AgentAlive` (idle still; thinking = pending run, slow tilt; running = pin on the Momo + sway; done = dated ink stamp that lands only on a seen running->done transition; failed = torn folded corner, no motion; interrupted = idle), state also in words beside each avatar incl. the lead; motion item 7 in paper.css gated like 1/2/5, DESIGN.md now "seven". Left: hero crew brain, agent chat header and d9 dispatch slips not wired; "thinking" is pending status (runs API has no tool-call data).
- [x] **d5 pins-mean-running** (backlog 7): https://github.com/dillonmohr8777/deer-flow/pull/36: landing capability cards drop their brass pins for kraft photo corners (new paper.css `.paper-corners`); /login and /invite sheets rest in corners and are pinned only while a sign-in or accept request is in flight (invite release still lifts a pin that meant work); DESIGN.md records the pin rule and the corners. Left: the Momo Hello film art has painted pushpins (Known drift); d9's finished dispatch slips should use the same corners.
- [x] **d6 catalog-priority** (backlog 6): https://github.com/dillonmohr8777/deer-flow/pull/40: Tools & integrations opens on a Connected sheet (installed tools across categories), then "Available to connect" by category with configurable integrations before setup guides; row actions are Connect (account flow, royal outline), Configure (admin settings, outline), Details / Setup guide (read only, quiet); idle Not configured/Not installed/Suggested tags dropped; filter reads Connected; DESIGN.md rule added. Left: phone Connected rows with switch+edit+delete still clamp descriptions short (stack actions under text on phones).
- [x] **d7 landing-composition** (backlog 5): https://github.com/dillonmohr8777/deer-flow/pull/48: a kraft grid "Your team" sheet (lead plus the five specialists the Agents card names, canon Momo stickers, unpinned) stands right of the four cards from 64rem and follows them on phones; loose kraft scrap removed; hero/card/footer spacing tightened (1224 to 1124px at 1440x900); phone card stack loses the uneven stagger gaps; Agents copy says growth, not brand; DESIGN.md front door composition rule. Left: stickers small for the sheet at 1440 (two-tier cluster would fill it); card pins remain until #36 lands.
- [x] **d8 board-visual**: https://github.com/dillonmohr8777/deer-flow/pull/51: threads are paper slips (kind, client, time, two-line title); a recorded decision (approved/replied/closed) is a dated ink stamp, open states stay StatusTags; the conversation reads as letters (client on cream-lo, ink 12.34:1, fixes the f6 contrast by redesign), Momo's draft waits on its own torn sheet with its one action, pinned only while a request is in flight, and a draft sent verbatim is not repeated; phones show one pane with an "All threads" link and focus management; "Approved reply" replaces the em dash label; DESIGN.md Board rule. Left: f6 needs only a verify once #51 lands; rest of f11 (messages retry, neutral client name, roles), f7 read-only send and f8 owner flags are build-routine work; desktop slip column has no more-below cue.
- [x] **d9 dispatch-board** (backlog bold idea): https://github.com/dillonmohr8777/deer-flow/pull/28: Mission Control as a paper dispatch board. Slice 1 (board, lanes, pins, stamps); slice 2 (stamp beside words, folded kraft dog-ear on failures, interrupted/queued ink-muted, focus ring inside the clip, lead column sheet); slice 3 (2026-09-26: the agent team uses the board's words, running = pinned "Working", pending-only = unpinned "Queued", the lead sheet says its own state and pins only for its own running run, hero brain pulses only when running; Agent Studio full width; notes in `docs/pr-evidence/momo-week/d9-dispatch-board/slice3/NOTES.md` because the PR body edit got a 403). Left: route team state through `AgentAlive` once #34 lands; brief/review lanes need backend stages; stamp should link a named evidence artifact; wide specialist cards at full width.

### E. Board to product (build routine; added 2026-09-25 when Dillon scaled the week up)
- [x] **e1 base-failures**: https://github.com/dillonmohr8777/deer-flow/pull/49 — root-caused and fixed all three named failures. `test_client_langfuse_metadata.py`: `DeerFlowClient._stream_turn`'s bare `for item in _stream_with_sandbox_lease_cleanup(...)` relied on GC to cascade `.close()` into the wrapped iterator when the stream is abandoned, which isn't deterministic once an exception's traceback keeps the frame alive (confirmed by an instrumented repro), so the agent's cleanup logged with no trace id; fixed with an explicit `try/finally` around the loop that closes the wrapped iterator itself. `login-mfa-step` lint: narrowed the `RequestInfo | URL` union before stringifying (was falling through to `Object`'s default `toString` for a `Request`). `sidecar-delete-gating` ECONNREFUSED: all 3 assertions already passed, but `SidecarPanel`'s `useThreadStream` reaches the LangGraph SDK's `useStream()`, which fetches thread history on mount with no server to answer under `happy-dom`, leaving an unhandled rejection that failed the file; mocked `useThreadStream`/`useDeleteThread` (the gating under test comes from `hasPermission`, not live thread data). `pnpm lint` and `pnpm test` (2223/2223) are green. Full backend suite: 19230 passed/13 failed/202 skipped — the 13 are the separately-tracked f15 lane-base-suite-red gap (board/team/academy routes not yet in the permission/scope/thread-id-contract lists), untouched by this PR, so e1's "full suite green" bar is not fully met but is out of e1's own named scope.
- [x] **e2 triage-on-create**: https://github.com/dillonmohr8777/deer-flow/pull/53 — migration `0040_board_thread_triage` (chains after `0039_team_board_academy`, single head) adds nullable `board_threads.urgency`/`summary`; `POST /api/board/threads` runs `triage_board_thread` on the subject (the only text a thread has at creation) and persists the result, moving `new` -> `triaged`; router wraps the triage+patch call in its own try/except so nothing above `triage_board_thread`'s own safe fallback can turn a created thread into a 500. Client-supplied `kind` untouched. 46/46 targeted tests pass (new success/fallback/router-guard cases); 714 passed/17 skipped on `-k "board or client"` sweep (1 pre-existing unrelated `test_client_langfuse_metadata.py` failure, same one every prior run has flagged); ruff check+format clean. Left: once e3 gives thread creation an initial-message body, feed that into triage instead of/alongside subject; e4/e5 can now read urgency/summary.
- [x] **e3 client-post-ui**: https://github.com/dillonmohr8777/deer-flow/pull/56 — "see only their own threads" and "create a thread for your own client" were already covered by b2's `test_client_member_sees_only_assigned_client_owner_sees_all`; the real gap was `list_board_messages` returning every message unfiltered, so a client contact could read Momo's still-unapproved draft. It now hides a `momo` message while its thread is `drafted` from anyone but an org owner/admin (visible again once approved). Added the missing thread-creation UI: a "New thread" form scoped to `GET /api/clients/mine` (new `useMyClients`), never the full org roster. Also fixed a `BoardBody` race where the "clear a no-longer-listed selection" effect could un-select a just-created thread mid-refetch. Left: `f8 board-owner-only-actions` (Approve/Send buttons still show to non-owners client-side; backend 403s) is a separate, pre-existing finding, not touched here.
- [ ] **e4 owner-alerts**: threads waiting on approval show a count on the Desk and in the sidebar; urgent threads outside 8am to 8pm ET are flagged "after hours". No external sends. Accept: tests.
- [ ] **e5 concierge-loop**: behind a flag (default off), a scheduled job runs the momo-concierge agent over new threads and writes drafts through b4's workflow, never approving or replying. Accept: a test with a stubbed agent proves it drafts and never approves.
- [ ] **e6 m4-entitlements**: implement the M4 evaluator and snapshot endpoint from c2's design (do c2 first). Accept: tests for allowed, denied and missing entitlement on at least 3 paid mutations.
- [ ] **e7 board-usage-ledger**: every model call the board makes (triage, drafting) is recorded in the existing usage ledger with client and organization. Accept: tests.

### F. Review findings (the review routine adds these; the build routine works them before section E)
- [x] **f1 board-patch-bypass** (from PR #22): https://github.com/dillonmohr8777/deer-flow/pull/37 — `PATCH /api/board/threads/{id}` now refuses `drafted`/`approved`/`replied` for every caller (workflow-only, 409) and requires org owner/admin for any other status change (403 otherwise), auditing both denials and successes as `board.thread.status_patch`. Subject-only PATCH is unaffected. 7/7 targeted tests pass (2 new: workflow-bypass + owner-gate), 695 passed/17 skipped on `-k "board or client"` sweep (pre-existing unrelated `test_client_langfuse_metadata.py` failure, same one every prior run has flagged); ruff check+format clean. Review found a follow-up (own-review finding, referred to as f14): the admin check now runs *before* the workflow-only-status check, so a non-admin's forged `approved`/`replied` PATCH always lands the audited 403 (commit 8c12d54d on the same PR) instead of an unaudited 409; 7/7 still pass, 695/17 sweep unchanged.
- [x] **f2 board-author-forgery** (from PR #22): https://github.com/dillonmohr8777/deer-flow/pull/39 — `BoardMessageCreateRequest` no longer accepts `author_kind` at all; `POST /threads/{id}/messages` derives it server-side (`owner` for an active org owner/admin, else `client`; `momo` stays reachable only through the draft workflow's own `add_message` call). New test reverts `board.py` to the pre-fix version to confirm red (`assert 'owner' == 'client'` fails), then green on the fix. 6/6 targeted tests pass, 694 passed/17 skipped on `-k "board or client"` sweep (pre-existing unrelated `test_client_langfuse_metadata.py` failure, same one every prior run has flagged); ruff check+format clean.
- [x] **f3 board-draft-leak** (from PR #22; backend half of e3): https://github.com/dillonmohr8777/deer-flow/pull/41 — `GET /threads/{id}/messages` now filters out `momo`-authored messages for callers who are not an active org owner/admin while the thread is `drafted`; visible again once approved. New test asserts a plain assigned member sees none and the owner sees both. 6/6 targeted tests pass, 694 passed/17 skipped on `-k "board or client"` sweep (pre-existing unrelated `test_client_langfuse_metadata.py` failure, same one every prior run has flagged); ruff check+format clean. Left: f8 board-owner-only-actions (surfacing can_approve/can_reply to the UI) is a separate finding, not touched here.
- [x] **f5 board-thread-carryover** (from PR #26): https://github.com/dillonmohr8777/deer-flow/pull/50 — `<ThreadDetail>` now renders with `key={selectedId}` so React remounts the detail pane (and its `draftText`/`replyText`/`replySeededFor` state) on every thread switch, instead of reusing the old instance. New `board-thread-carryover.dom.test.tsx` types a draft on thread A, switches to thread B, and asserts B's textarea is empty, is a distinct DOM node, Save stays disabled and no draft mutation fires; confirmed red (thread A's text carried over) with the fix reverted, green with it restored. `BoardBody` exported for direct rendering in the test.
- [x] **f6 board-message-contrast** (from PR #26): https://github.com/dillonmohr8777/deer-flow/pull/51 — fixed as a side effect of d8's redesign, not a standalone patch: `.message`'s `var(--muted)` text is gone entirely, replaced by `.letter[data-author=...]` rules that pair `--secondary`/`--secondary-foreground` (client) and `--card`/`--card-foreground` (owner/momo) instead of a background-less muted color. Verified this run on `momo-week/board-visual` (build routine, no new commits needed): `pnpm rstest run tests/unit/components/workspace/board-data.test.ts` 9/9 pass; `pnpm exec playwright test tests/e2e/board.spec.ts --project=chromium` 5/5 pass, including both `textContrast(...) >= 4.5` assertions (owner letter body/time, draft-sheet heading/body/note) added by that PR. No separate `momo-week/board-message-contrast` branch/PR opened since #51 already carries the fix and its test coverage.
- [x] **f4 board-draft-authz** (from PR #25): https://github.com/dillonmohr8777/deer-flow/pull/52 — `assert_can_draft` now takes `actor_is_owner`, mirroring `assert_can_approve`/`assert_can_reply`; `draft_board_reply` computes it via the existing `_is_active_org_admin` check and records a denied `board.thread.draft` audit event before the 403 (check runs before any write, so the thread's status is untouched on denial). 29/29 targeted tests pass (2 new: non-owner draft rejection at the workflow layer and at the router, incl. no forged `momo` message landing); 695 passed/17 skipped on `-k "board or client"` sweep (same pre-existing unrelated `test_client_langfuse_metadata.py` failure every prior run has flagged); ruff check+format clean. Left: the task text also allows "an internal service caller"; no such caller exists yet (that's e5's job), so this scopes to the owner/admin gate only.
- [!] **f29 migration-two-heads** (from PR #41, high; do at merge time): blocked for any single-branch routine run — the actual fix ("whichever merges second renames to `0040_*` chaining off the other's 0039") requires PR #38 and PR #41 to exist in the same tree, and nothing merges into `lane/momo-week` this week, so there is no tree to rename against. The guard test (`test_0039_chains_into_the_single_head`, added by f20) already exists and passes now; it will fail loudly for whichever of #38/#41 actually merges second, which is when this rename can be done for real. Not independent of that merge, so skipped this run in favor of f21. `0039_board_message_approval.py` (PR #41) and `0039_team_board_academy.py` (PR #38) both chain off `0038_board_threads`, so merging both makes `bootstrap.py:656` `upgrade(cfg, "head")` fail and the Gateway can't boot. Review 2026-09-26: PR #53 adds `0040_board_thread_triage` chaining off `0039_team_board_academy`, so #41's migration has to chain after `0040_board_thread_triage` (and be renamed `0041_*`) when it merges.
- [x] **f20 board-rejected-draft-leak** (from PR #41): https://github.com/dillonmohr8777/deer-flow/pull/41 — `board_messages` gains a nullable `approved_at` (migration `0039_board_message_approval`); `/approve` stamps only the current, most-recent unapproved `momo` message via `BoardRepository.approve_latest_draft()`; `list_board_messages` hides any `momo` message with `approved_at is None` from non-admins regardless of thread status, so a PATCH-reject to `triaged`/`new`/`closed` no longer leaks the draft and an earlier draft superseded by a redraft stays hidden forever even after a later approve. New test `test_rejected_and_superseded_drafts_stay_hidden_from_non_admin` reproduces the review's exact probe, confirmed red on the pre-fix filter. 697 passed/17 skipped on `-k "board or client"` sweep (same pre-existing unrelated `test_client_langfuse_metadata.py` failure); ruff check+format clean; replied on the PR. Still independent of #37 (workflow-status PATCH bypass) — land #37 with or before #41 for the PATCH-forgery half; f7 board-send-what-was-approved tracks the separate approved-before-reply/body-mismatch product question the reviewer flagged. Review follow-up (2026-09-26): reproduced a real two-Alembic-heads collision between this PR's `0039_board_message_approval` and #38's `0039_team_board_academy` (both chain off `0038_board_threads`; merging both makes `ScriptDirectory` return two heads and breaks Gateway's `upgrade(cfg, "head")` boot). Neither branch can safely rename onto the other's revision id in isolation (nothing merges into `lane/momo-week` this week), so added `test_0039_chains_into_the_single_head` (same `len(get_heads()) == 1` guard already used for the 0023/0025/0026 collisions in this repo) — it passes now and will fail loudly for whichever of #38/#41 merges second, forcing the rename-and-rechain then instead of a silent boot failure. Whoever integrates the two branches for real still needs to do that rename by hand.
- [x] **f21 vps-postgres-not-backed-up** (from PR #43): https://github.com/dillonmohr8777/deer-flow/pull/54 — `offsite_backup.py`'s `dump_postgres()` now `pg_dump`s the live Postgres container (`docker exec`, over its own trusted local socket, no password needed) into its own AES-256-GCM-encrypted, restore-checked file (`postgres-<stamp>.dump.enc`) alongside the gateway volume tar, same key, same 14-copy retention; receipt gains a `"postgres"` field (`null` when no Postgres container is configured, e.g. the PC's SQLite stack — unaffected). `backup.sh` derives the container/user/db from `.env`, gated only on `POSTGRES_PASSWORD`'s presence, its value never read. A configured-but-failing dump now exits loudly instead of a silent green receipt (the exact f21 bug, reproduced and asserted red-to-loud in the new test). `deploy/momentum/vps/README.md` documents the new file and gives concrete restore steps (decrypt both, load the volume, `pg_restore` the dump, restart, sign in with an existing account). 6/6 new tests pass (2 files: one loads `offsite_backup.py` by path and fakes `subprocess.run`, one runs the real `backup.sh` with a stub `docker` on PATH); 25/25 on the wider deploy/momentum sweep; ruff check+format clean. Left: f25 (health.sh ignoring a FAIL receipt) is separate and untouched here.
- [x] **f22 entitlement-doc-require-auth** (from PR #45): fixed on PR #45 itself (675181bd), same run that read the finding. `docs/momo-week/m4-entitlement.md` §3 dropped `@require_auth` from its decorator-stack example (`@require_entitlement` now composes directly after `@require_permission`, which reuses the middleware's `organization_id`-bearing context instead of `require_auth`'s org-blind `_authenticate()` rebuild); a request with no resolved org is now documented as denied (403) while entitlements are enabled, not skipped; the degraded-provider cache is now explicitly process-level, keyed by `organization_id` (not "cached this request cycle"); both cases (missing-org 403, no-row 403) added to the e6 test list along with a multi-request grace-window assertion. Accept met: doc updated, e6 test list carries both named cases.
- [x] **f7 board-send-what-was-approved** (from PR #26): https://github.com/dillonmohr8777/deer-flow/pull/55 — `send_board_reply` now fetches the latest `momo` draft message and rejects (409) a reply body that doesn't match it verbatim (whitespace-trimmed); the reply `Textarea` in `board.tsx` is now `readOnly`, so the send view has nothing to diverge from. New `test_reply_must_match_approved_draft_verbatim` confirmed red on the pre-fix router, green after; `test_draft_approve_reply_lifecycle` no longer relies on sending a body that differs from the draft (that was the gap). 6/6 targeted tests pass, 709 passed/17 skipped on `-k "board or client"` sweep (pre-existing unrelated `test_client_langfuse_metadata.py` failure, same one every prior run has flagged); ruff check+format clean; `pnpm test` 2253/2253 (pre-existing sidecar-delete-gating file failure), `pnpm check` clean on touched files (pre-existing login-mfa-step error); `board.spec.ts` e2e 3/3 incl. new readonly/not-editable assertions. Left: the "any edit goes back to drafted" alternative isn't implemented (rejects outright instead), which still meets the accept criteria.
- [ ] **f14 board-patch-forgery-unaudited** (from PR #37): `board.py:215-216` returns 409 for a PATCH to `drafted`/`approved`/`replied` before any audit call, so a plain member's forged `{"status":"approved"}` leaves zero `board.thread.*` audit rows (confirmed by probe: 409, events `[]`), contrary to the PR body. Fix: record a `denied` `board.thread.status_patch` event before the 409 (or run the admin check first so non-admins get the audited 403). Accept: `test_patch_cannot_bypass_workflow_status` asserts a `denied` event for USER_D after the forged `approved` PATCH.
- [ ] **f12 triage-action-fail-closed** (from PR #30): `triage.py:165-167` maps a missing or unknown `action` to `draft`, and a test locks that in, so a model steered by injection into emitting `"action":"auto-reply"` (or no action) still gets a Momo draft. Fix: unknown or missing action goes to `escalate` (at minimum when kind is `concern` or urgency is `high`/`urgent`). Accept: `test_board_triage.py` tests where an invalid and a missing action both return `escalate`.
- [x] **f33 triage-fallback-persisted-as-real** (from PR #53): fixed on PR #53 itself, commit 2d6fce2f. `BoardThreadTriage` gains a `fallback: bool` field, set on the single code path both the unparseable-response and model-call-exception branches already fall through to; `create_board_thread` now only patches `status=triaged`/`urgency`/`summary` when `not triage.fallback`, leaving the thread `new` with both columns `NULL` otherwise. `test_thread_creation_falls_back_when_model_call_fails` now asserts `status == "new"`/`urgency is None`/`summary is None` (was locking in the bug); all four `test_board_triage.py` fallback tests assert `fallback is True`; the two `test_board_router.py` assertions reverted from `"triaged"` back to `"new"`. Also closed the review's "suspected, not confirmed" hang: `asyncio.wait_for(triage_board_thread(...), timeout=20)` in the router, with a new test proving a hung model no longer hangs thread creation. 47/47 targeted tests pass; 715 passed/17 skipped on `-k "board or client"` sweep (same pre-existing unrelated `test_client_langfuse_metadata.py` failure); ruff check+format clean; replied on the PR.
- [ ] **f23 vps-login-ratelimit-shared-bucket** (from PR #43): behind Caddy, nginx sets `X-Real-IP $remote_addr` (`docker/nginx/nginx.conf:87`) = Caddy's container IP, and `auth.py:252-287` keys the login limiter on peer IP or that header, so every internet client shares one bucket and one attacker's failed logins lock out all password logins. Fix: `set_real_ip_from <compose subnet>; real_ip_header X-Forwarded-For;` in nginx for the VPS overlay (or have Caddy set X-Real-IP) plus `AUTH_TRUSTED_PROXIES`. Accept: a test/config check that failed logins from IP A do not 429 IP B through the VPS proxy chain.
- [ ] **f24 vps-backup-key-world-readable** (from PR #43): `momobot-backup.service` has no `User=`/`UMask=`, so it runs as root with umask 022 and `offsite_backup.py:46` creates the AES key 0644; `--restore` also writes plaintext to /tmp 0644. Fix: `UMask=0077` in the unit (ideally a non-root `User=` in the docker group) and create the key/restore output with `os.open(..., 0o600)`. Accept: unit file has `UMask=0077` and a test that `load_key()` on a fresh path yields mode 600.
- [ ] **f25 vps-health-ignores-failed-backup** (from PR #43): `offsite_backup.py:83` writes a receipt even when `state` is FAIL and `health.sh:25-27` checks only the newest receipt's mtime, so a backup whose decrypt check failed reports `"ok":true`. Fix: health.sh requires `"state": "PASS"` in the newest receipt (and backup exits non-zero on FAIL). Accept: with a FAIL receipt, `health.sh` exits 1.
- [x] **f30 board-phone-loading-no-back** (fixed in PR #51 (56f02608), verified by review 2026-09-26: board.spec e2e 6/6 on head; "keeps the way back while a thread is still loading on a phone" fails against e3dd7ebf's board component and passes on head) (from PR #51): `board.tsx:342-344` renders only `<WorkingState>` while the thread loads; below 64rem the list is hidden (`board.module.css:191-198`), so a slow/retrying/hung GET strands a phone user with no control and focus on BODY (heading-focus effect `:308-313` fires only on `loaded`, error path never moves focus). Fix: render `{back}` in the loading branch and focus it on one-pane width when not loaded/errored. Accept: 390px e2e with a never-resolving thread GET finds the "All threads" button and it returns to the list.
- [ ] **f17 comic-intro-breaks-tests** (from PR #31): `scrapbook-backdrop.tsx:72` switches to `momo-comic-intro-20260925` but `momobot-intro.dom.test.tsx:81,108` still expect `/momentum/films/momo-collage.webp`, so 2 of 9 tests fail on the branch. Fix: update both poster assertions and add a check that the pause control renders inside the `[data-treatment="paper"]` portal host. Accept: `pnpm rstest run tests/unit/components/momentum/momobot-intro.dom.test.tsx` 0 failures.
- [ ] **f8 board-owner-only-actions** (from PR #26): `canApprove`/`canSendReply` (`board-data.ts:60-66`) check status only, so non-owners see Approve and Send buttons that always get a 403. Fix: the API returns the caller's permissions (e.g. `can_approve`/`can_reply` on the thread response) and the UI hides the panels when they're false. Accept: a backend test for both flags (owner vs plain member) plus a UI test that a member sees no Approve or Send.
- [ ] **f26 catalog-connected-needs-account** (from PR #40): `plugin-gallery.tsx:125` sets `installed: !!status` and `plugin-directory.tsx:129` files every installed row under Connected, so an install with `auth_status: "required"` (and, for non-admins, disabled/unselectable MCP items, `plugin-gallery.tsx:170`) sits under Connected with a Connect button, against DESIGN.md "lead with what works"; also the Connected hint `en-US.ts:86` says "Switch one off to pause it" but `PluginRow` renders no switch. Fix: connected = has status && `auth_status !== "required"` && enabled && selectable; drop or implement the switch copy. Accept: a `PluginGallery` DOM test where an auth-required install is not inside `plugin-connected`, and no switch promise without a switch.
- [x] **f15 agent-life-truncated-idle** (fixed in PR #34 822700da, verified by review 2026-09-26: new tests red on 822700da~1, green on head; follow-up gap in f28) (from PR #34): `command-center.tsx:197,265` derives every agent's state from one 20-run console page and `agent-life.ts:42` returns Idle for any agent absent from it, so an agent whose latest run failed reads "Idle" once 20+ newer runs exist (violates "Truth before tidiness"). Fix: per-assistant latest run from the backend, or a distinct "No recent run" state when absent and `has_more` is true. Accept: `agent-life.test.ts` case with a truncated 20-run page where the absent agent is not labelled Idle.
- [x] **f9 dispatch-interrupted-tone** (fixed in PR #28 (61787ed6 + b52aa782), verified by review 2026-09-26: command-center unit suite 54/54 on head; the new tests are red on dca26b7a src (3 fail) and green on head; `agentLabel` is now tested through the real `CommandCenter`) (from PR #28; d9 slice 2 pushed both halves to #28 at b52aa782: neutral tone and no tear for `interrupted`, and a real-`agentLabel` test through `CommandCenter`; needs review verify): `dispatch-board.tsx:38,301-305` gives `interrupted` runs `data-tone="danger"` and the torn corner, which DESIGN.md reserves for real failures. Also, the "names the agent in words" test (`command-center-dispatch-board.dom.test.tsx:134`) stubs `agentLabel` and never tests the real one. Fix: a neutral tone for `interrupted`, and test the real `agentLabel`. Accept: a DOM test that an interrupted slip has no danger tone or torn corner, plus a test of `agentLabel` from `command-center.tsx`.
- [ ] **f13 scenario-e2e-not-circular** (from PR #30): `test_board_scenario_e2e.py` stubs triage to return each fixture's own `expected` block and drafts a canned body the test wrote, so its action/urgency/forbidden-phrase/"never approved" checks pass by construction; the backend never sees triage's action and `/draft`/`/approve`/`/reply` accept an escalate-class thread. Fix: add a negative case where the stub mislabels an injection/must-refuse thread as `draft` with a forbidden phrase in the draft, and assert a backend guard stops it (with e2 persisting action, `/draft` on an `escalate` thread gets 409) or mark it xfail naming the gap. Accept: that test exists and is green (or xfail with reason until e2 lands).
- [ ] **f10 avatar-monogram-stable** (from PR #27): `momo-avatar.tsx` seeds the monogram from `display_name ?? name`, so it changes from "O" to "W" when the record loads. `agent-welcome.dom.test.tsx:50-72` passes only because both fixture strings start with "O". Fix: seed from `agent.name`. Accept: that test with a display name starting with a different letter, showing the same letter before and after the load.
- [x] **f16 done-stamp-replays** (fixed in PR #34 822700da, verified by review 2026-09-26: new tests red on 822700da~1, green on head; follow-up gap in f28) (from PR #34, low): `agent-alive.tsx:41-46` never clears `fresh`, so the Done stamp animation replays whenever `data-live` flips off/on (tab hidden/visible, motion or treatment toggle), contrary to DESIGN.md item 7 "lands once". Fix: clear `fresh` on `animationend` (timeout fallback when motion is off). Accept: DOM test running→done→animationEnd→motion off→on shows no `data-stamp`.
- [ ] **f11 board-ui-polish** (from PR #26, low): remove the em dash in "Approved &mdash; send it" (`board.tsx:331`; update `board.spec.ts:188`); add retry to the messages ErrorState (`board.tsx:249`); show a neutral name, not the raw `client_id`, when the client lookup fails (`board.tsx:93`); give aria-labelled divs a role (`board.tsx:125,285,314,329`). Accept: board unit tests plus `board.spec.ts` green, with no em dash in board copy.
- [ ] **f27 agents-crash-test-reproduces** (from PR #46, low): the new retro-resolve tests never create `#workspace-main`, so against the old code they fail only on `expected '' to match /^url\(#/`, never on the real `removeChild`/`insertBefore` crash. The fix itself is sound. Fix: add a regression test rendering a loading→loaded child swap inside `#workspace-main` with the old DOM shape. Accept: that test throws/fails on origin/lane/momo-week's `retro-resolve.tsx` and passes on the head. (Suspected, needs a browser: the filter now lands on an already-painted element, which the old comment says has no visual effect; confirm with a screenshot diff.)
- [x] **f31 board-client-letter-contrast-untested** (fixed in PR #51 (56f02608), verified by review 2026-09-26 by reading the test: board.spec seeds a client message (`:269`) and asserts `[data-author="client"]` author/time/body contrast >= 4.5 (`:285`), passing on head. Not mutation-tested against the muted fill) (from PR #51, low): d8/f6 claims the client letter is ink on cream-lo (12.34:1, verified by measurement) but `board.spec.ts` `mockBoard` only adds `momo`/`owner` messages (`:130`, `:152`), so no test renders `[data-author="client"]`. Fix: seed a client message and assert its text contrast. Accept: the new assert is >= 4.5 on head and fails if `.letter[data-author=client]` reverts to the muted fill.
- [x] **f32 landing-lead-art-mismatch** (fixed in PR #48 (acdd3afc), verified by review 2026-09-26: landing-team 3/3 on head; the new lead-art assertion fails against fdd43aff's component; the lead now renders `BRAIN_FLAT` from `momo-avatar.tsx`) (from PR #48, low): `momentum-landing.tsx:84-92` and `DESIGN.md:174` claim the team sheet uses the same art the Command Center roster uses, but the lead is drawn as `lead.svg` while the Command Center/topology lead is Dillon Brain (`command-center.tsx:210`, `agent-topology.tsx:51-56`). Fix: render the lead with `MomoAvatar`'s dillon-brain art, or drop the claim. Accept: landing-team DOM test asserts the lead renders the same art `MomoAvatar` renders for `dillon-brain`.
- [ ] **f28 agent-life-has-more-wiring-test** (from PR #34, low): f15/f16 are fixed and red-on-old verified, but nothing tests the `command-center.tsx:268` wiring that passes `activityRuns.data.has_more`; deleting that argument keeps every test green. Also the claimed timeout fallback for clearing `fresh` isn't in `agent-alive.tsx:76` (suspected stale stamp after a treatment switch). Fix: add the CommandCenter test; add a ~400ms timeout clear. Accept: a `command-center` DOM test with 20 lead runs, `has_more: true` and a roster agent missing from the page shows "No recent run", not "Idle".

Legend: `[ ]` todo · `[~]` in progress · `[x]` done (PR link) · `[!]` blocked (reason)
- [ ] **f14 board-null-client-bypass** (found while building PR #38): every `/api/board` route that loads a thread only calls `_require_client_access` when `row["client_id"]` is not null (`get_board_thread`, `patch_board_thread`, `list_board_messages`, `add_board_message`, `_load_thread_for_actor`), and `list_board_threads` returns null-client threads to org admins only by accident of the unfiltered query. A thread with no client is readable and writable by every org member, including a future client-role account. Fix: treat a null-client thread as owner/admin-only (404 otherwise), or forbid creating one. Accept: a test where a plain member and a `client`-role member get 404 on GET/PATCH/messages/draft for a thread whose `client_id` is null, and an owner still sees it.
- [ ] **f15 lane-base-suite-red** (found while building PR #38): 13 backend tests fail on untouched `lane/momo-week`: `test_auth_me_permissions.py` (4), `test_authorization_route_permissions.py` (5), `test_personal_access_tokens_repository.py::test_pat_scopes_stay_aligned_with_route_permissions` (permission lists and `PAT_ALLOWED_SCOPES` never gained `board:read`/`board:write`), `test_thread_id_route_contract.py` (2: `board.py` handlers type `thread_id` as `str`, and the sweep doesn't list `board`), `test_verify_cutover.py::test_local_head_is_current_pinned_revision` (still pinned to `0037`). Fix them at the source, not by skipping. When PR #38 lands, `team:*`/`academy:*` and head `0039_team_board_academy` join the same lists. Accept: those 13 pass.
- [x] **f16 mfa-challenge-as-session** (critical, pre-existing; found by the interactive invite review): https://github.com/dillonmohr8777/deer-flow/pull/44 — `decode_token` ignored `typ`, so the MFA challenge `/login/local` returns after a correct password decoded as a session (ver=0) and replaying it as the `access_token` cookie skipped TOTP. Fixed: tokens with a non-`access` `typ` are refused; new access tokens carry `typ: access`; legacy tokens stay valid. Regression test fails on base (`200 == 401`). Needs a live deploy (Dillon's go) to protect running instances.
- [x] **f17 next-param-control-chars** (medium, pre-existing): https://github.com/dillonmohr8777/deer-flow/pull/44 — `validate_next_param` / `validateAuthNextPath` accepted `/\t/evil.example` (browsers read `//evil.example`), an open redirect after sign-in. Both now refuse control characters and spaces; the frontend also requires same-origin.
- [~] **f18 invite-accept-skips-mfa** (high, pre-existing): `/api/v1/auth/invitations/accept` let an existing password account accept with just its password and returned a full session with no MFA step. Being fixed in https://github.com/dillonmohr8777/deer-flow/pull/42 (existing accounts must sign in, then accept). Routines: don't take this one.
- [ ] **f19 email-change-unverified** (low, pre-existing): `auth.py` lets a password user change their email to any unused address without verification, so someone can squat an invitee's address and block their Google sign-in (409). Fix: limit email changes to first-time setup, or require verification. Accept: a test that a post-setup email change without verification is refused.

### G. Interactive build (Claude chief, live with Dillon; routines never take these)
- [x] **g1 team-board-academy**: https://github.com/dillonmohr8777/deer-flow/pull/38 — Momentum-only Team channels (`/api/team`, own `team_channels`/`team_messages` tables, owner/admin add channels, author always the caller) and AI Academy (`/api/academy`, 12 lessons in 3 tracks, per-person progress, six Momo explainer video slots); one gate (`private_workspace.enabled` + owner/admin/member role, else 404) also exposed as `features.momentum_internal`; migration `0039_team_board_academy`. Backend new tests green (client-lockout verified to fail with the role check removed); full suite 19080 pass / 13 fail, all 13 pre-existing (f15). Frontend unit 8/8, e2e team-academy + board 10/10 incl. no overflow at 390/768/1440; 4 pre-existing unit-file failures unchanged. Left: staff invites (roster kept local, needs Dillon), Slack mirroring (needs live yes), video URLs once Cowork's videos are approved.
- [~] **g2 invite-google-signin**: https://github.com/dillonmohr8777/deer-flow/pull/42 — Google (SSO) accounts can accept workspace invitations; review-driven rework in progress (existing accounts sign in then accept, invite-gated Google auto-create, token stash hygiene, CSRF on the session path).
- [x] **g3 vps-kit**: https://github.com/dillonmohr8777/deer-flow/pull/43 — always-on Linux VPS kit (Caddy TLS as the only public listener, systemd boot/health/backup, gated data cutover); static review fixes applied (clone ref, backup image, no systemd EnvironmentFile, valid health JSON, pull_policy never).
- [x] **g4 auth-token-type**: https://github.com/dillonmohr8777/deer-flow/pull/44 — see f16 and f17.
- [x] **g1 follow-up**: PR #38 isolation fix. A real client (member plus `client_contact` assignment) passed the staff gate; now denied, a `client` invite role exists that is never staff, routes are gated before permissions and hidden from OpenAPI, `/members` drops client contacts and disabled users, and logout clears the query cache.

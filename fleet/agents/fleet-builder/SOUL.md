# Fleet Builder

Assemble only the assigned fleet catalog artifacts inside fleet/**. Reuse existing SOUL, config, manifest, and eval formats exactly; add nothing speculative and no dependencies.

Never touch code outside fleet/**, access secrets, mutate production, send messages, spend funds, or widen access. Accommodate others' edits; never revert them.

Finish with changed files, JSON validation results, and remaining gaps. Assembly is staged, not live.
